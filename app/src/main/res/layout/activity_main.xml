<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_bg"
    android:fillViewport="true"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Header -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="#FFFFFF"
            android:textSize="32sp"
            android:textStyle="bold"
            android:layout_marginTop="30dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_subtitle"
            android:textColor="#E0E0E0"
            android:textSize="16sp"
            android:layout_marginBottom="30dp"/>

        <!-- Sound Controls Container -->
        <!-- We reuse the layout structure for simplicity -->

        <!-- RAIN -->
        <LinearLayout style="@style/GlassCardStyle">
            <Switch
                android:id="@+id/switchRain"
                style="@style/SwitchStyle"
                android:text="@string/sound_rain"/>
            <SeekBar
                android:id="@+id/seekRain"
                style="@style/SeekBarStyle"/>
        </LinearLayout>

        <!-- FOREST -->
        <LinearLayout style="@style/GlassCardStyle">
            <Switch
                android:id="@+id/switchForest"
                style="@style/SwitchStyle"
                android:text="@string/sound_forest"/>
            <SeekBar
                android:id="@+id/seekForest"
                style="@style/SeekBarStyle"/>
        </LinearLayout>

        <!-- FIRE -->
        <LinearLayout style="@style/GlassCardStyle">
            <Switch
                android:id="@+id/switchFire"
                style="@style/SwitchStyle"
                android:text="@string/sound_fire"/>
            <SeekBar
                android:id="@+id/seekFire"
                style="@style/SeekBarStyle"/>
        </LinearLayout>

        <!-- OCEAN -->
        <LinearLayout style="@style/GlassCardStyle">
            <Switch
                android:id="@+id/switchOcean"
                style="@style/SwitchStyle"
                android:text="@string/sound_ocean"/>
            <SeekBar
                android:id="@+id/seekOcean"
                style="@style/SeekBarStyle"/>
        </LinearLayout>

    </LinearLayout>
</ScrollView>